package tm;

public class TM {
    //Development plan: Use NFA as inspiration, 
    //Include logic to handle state management,transitions,tape and simulation logic.
    
    /** TM seven-tuple:
     *  Tape alphabet 
     *  string alphabet
     *  Set of States
     *  Initial state - will always be q0?
     *  Accept states
     *  reject state - TM encodings won't have reject state bc they would just modify the tape's content
     *  transitions
     */

    /** Possible functions:
     *  TM constructor (of course)
     *  addState
     *  acceptState (Will the final state always be the accept state?)
     *  start state (will always be the first state)
     *  addsigma
     *  addTransition
     *  readWrite
     *  nextState
     *  runTM
     */


}
